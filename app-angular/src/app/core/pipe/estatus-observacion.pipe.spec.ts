import { EstatusObservacionPipe } from './estatus-observacion.pipe';

describe('EstatusObservacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstatusObservacionPipe();
    expect(pipe).toBeTruthy();
  });
});
